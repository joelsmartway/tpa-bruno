info:
  name: Status
  type: http
  seq: 4

http:
  method: GET
  url: "{{back}}/v1/sessions/{{sessionId}}/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

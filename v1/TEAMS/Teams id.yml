info:
  name: Teams id
  type: http
  seq: 2

http:
  method: GET
  url: "{{back}}/v1/teams/{{teamId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

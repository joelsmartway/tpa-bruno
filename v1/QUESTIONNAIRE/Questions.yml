info:
  name: Questions
  type: http
  seq: 2

http:
  method: GET
  url: "{{back}}/v1/questionnaires"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Questionnaire end
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{back}}/v1/sessions/{{sessionId}}/questionnaire/end"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

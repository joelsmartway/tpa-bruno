info:
  name: Next session
  type: http
  seq: 4

http:
  method: GET
  url: "{{back}}/v1/teams/{{teamId}}/sessions/next"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Team users
  type: http
  seq: 5

http:
  method: GET
  url: "{{back}}/v1/teams/{{teamId}}/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

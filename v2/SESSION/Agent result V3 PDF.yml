info:
  name: Agent result V3 PDF
  type: http
  seq: 3

http:
  method: GET
  url: "{{back}}/v2/sessions/{{sessionId}}/agent_results/v3/pdf"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Agent result V3
  type: http
  seq: 2

http:
  method: GET
  url: "{{back_staging}}/v2/sessions/{{sessionId}}/agent_results/v3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

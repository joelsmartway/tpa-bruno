info:
  name: Sessions
  type: http
  seq: 3

http:
  method: GET
  url: "{{back}}/v1/teams/{{teamId}}/sessions"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

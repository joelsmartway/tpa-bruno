info:
  name: Expire token
  type: http
  seq: 2

http:
  method: GET
  url: "{{back}}/v2/helpers/token"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

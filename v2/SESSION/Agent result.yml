info:
  name: Agent result
  type: http
  seq: 1

http:
  method: GET
  url: "{{back}}/v2/sessions/{{sessionId}}/agent_results"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Organization id
  type: http
  seq: 1

http:
  method: GET
  url: "{{back_staging}}/v1/organizations/{{organizationId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

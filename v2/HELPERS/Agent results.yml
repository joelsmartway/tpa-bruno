info:
  name: Agent results
  type: http
  seq: 1

http:
  method: GET
  url: "{{back}}/v2/helpers/{{sessionId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

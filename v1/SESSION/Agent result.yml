info:
  name: Agent result
  type: http
  seq: 3

http:
  method: GET
  url: "{{back}}/v1/sessions/{{sessionId}}/agent_results"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

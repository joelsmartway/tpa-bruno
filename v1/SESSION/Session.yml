info:
  name: Session
  type: http
  seq: 2

http:
  method: GET
  url: "{{back_staging}}/v1/sessions/{{sessionId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
